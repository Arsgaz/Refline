<UserControl x:Class="Refline.Admin.Views.TeamDashboardPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:primitives="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationFramework"
             Background="#0B1220">
    <UserControl.Resources>
        <SolidColorBrush x:Key="PageBaseBrush" Color="#0B1220" />
        <SolidColorBrush x:Key="PageSecondaryBrush" Color="#10192B" />
        <SolidColorBrush x:Key="PanelBrush" Color="#111827" />
        <SolidColorBrush x:Key="PanelHoverBrush" Color="#162033" />
        <SolidColorBrush x:Key="PanelActiveBrush" Color="#11263A" />
        <SolidColorBrush x:Key="BorderBrushNeutral" Color="#1E2A3D" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#DDE6F1" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2DC7FF" />
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#63D8FF" />

        <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0B1220" Offset="0" />
            <GradientStop Color="#10192B" Offset="1" />
        </LinearGradientBrush>

        <Style x:Key="PagePanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="20" />
        </Style>

        <Style x:Key="SectionTitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>

        <Style x:Key="SectionDescriptionTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="MetricLabelTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style x:Key="MetricValueTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,10,0,0" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>

        <Style x:Key="ReflineActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="{StaticResource PanelBrush}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PanelHoverBrush}" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource PanelActiveBrush}" />
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PeriodToggleButton" TargetType="Button" BasedOn="{StaticResource ReflineActionButton}">
            <Setter Property="Height" Value="44" />
            <Setter Property="MinWidth" Value="96" />
        </Style>

        <Style x:Key="DayPeriodButtonStyle" TargetType="Button" BasedOn="{StaticResource PeriodToggleButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedPeriodKind}" Value="Day">
                    <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WeekPeriodButtonStyle" TargetType="Button" BasedOn="{StaticResource PeriodToggleButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedPeriodKind}" Value="Week">
                    <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MonthPeriodButtonStyle" TargetType="Button" BasedOn="{StaticResource PeriodToggleButton}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedPeriodKind}" Value="Month">
                    <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomPeriodButtonStyle" TargetType="Button" BasedOn="{StaticResource PeriodToggleButton}">
            <Setter Property="MinWidth" Value="128" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding SelectedPeriodKind}" Value="Custom">
                    <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ReflineActionButton}">
            <Setter Property="Height" Value="44" />
        </Style>

        <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ReflineActionButton}">
            <Setter Property="Height" Value="44" />
            <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
        </Style>

        <ControlTemplate x:Key="ReflineDatePickerToggleButton" TargetType="Button">
            <Border x:Name="ToggleBorder"
                    Background="{StaticResource PanelActiveBrush}"
                    BorderBrush="{StaticResource BorderBrushNeutral}"
                    BorderThickness="1,1,1,1"
                    CornerRadius="0,10,10,0">
                <TextBlock Text="▾"
                           FontSize="14"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource TextSecondaryBrush}" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}" />
                    <Setter TargetName="ToggleBorder" Property="Background" Value="{StaticResource PanelHoverBrush}" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter TargetName="ToggleBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="DarkDatePickerTextBoxStyle" TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DatePickerTextBox}">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10,0,0,10">
                            <Grid>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Margin="0"
                                              Padding="14,10"
                                              VerticalAlignment="Center"
                                              HorizontalScrollBarVisibility="Hidden"
                                              VerticalScrollBarVisibility="Hidden"
                                              Foreground="{TemplateBinding Foreground}" />
                                <TextBlock x:Name="Watermark"
                                           Margin="14,10"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           Opacity="0.8"
                                           Visibility="Collapsed"
                                           Text="{TemplateBinding Tag}" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}" />
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PanelHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Opacity" Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DarkDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Background" Value="{StaticResource PanelActiveBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Height" Value="44" />
            <Setter Property="MinWidth" Value="160" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DatePicker">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="40" />
                            </Grid.ColumnDefinitions>

                            <DatePickerTextBox x:Name="PART_TextBox"
                                               Grid.Column="0"
                                               Style="{StaticResource DarkDatePickerTextBoxStyle}"
                                               Background="{TemplateBinding Background}"
                                               Foreground="{TemplateBinding Foreground}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="{TemplateBinding BorderThickness}"
                                               VerticalContentAlignment="Center" />

                            <Button x:Name="PART_Button"
                                    Grid.Column="1"
                                    Focusable="False"
                                    Template="{StaticResource ReflineDatePickerToggleButton}" />

                            <Popup x:Name="PART_Popup"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                   PopupAnimation="Fade">
                                <Border Background="#FFFFFF"
                                        BorderBrush="{StaticResource BorderBrushNeutral}"
                                        BorderThickness="1"
                                        CornerRadius="8">
                                    <Calendar x:Name="PART_Calendar"
                                              Style="{StaticResource ReflineCalendarStyle}" />
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="0,0,4,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,22">
                    <TextBlock Text="{Binding ScopeTitle}"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="{Binding ScopeSubtitle}"
                               Margin="0,8,0,0"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextWrapping="Wrap" />
                </StackPanel>

                <Border Grid.Row="1"
                        Style="{StaticResource PagePanelBorderStyle}"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Период и фильтры"
                                   Style="{StaticResource SectionTitleTextStyle}" />
                        <TextBlock Text="Выберите период, настройте даты и обновите сводную аналитику команды."
                                   Style="{StaticResource SectionDescriptionTextStyle}"
                                   Margin="0,0,0,16" />

                        <WrapPanel>
                            <Button Content="День"
                                    Width="96"
                                    Margin="0,0,10,10"
                                    Style="{StaticResource DayPeriodButtonStyle}"
                                    Command="{Binding SetDayPeriodCommand}" />
                            <Button Content="Неделя"
                                    Width="108"
                                    Margin="0,0,10,10"
                                    Style="{StaticResource WeekPeriodButtonStyle}"
                                    Command="{Binding SetWeekPeriodCommand}" />
                            <Button Content="Месяц"
                                    Width="108"
                                    Margin="0,0,10,10"
                                    Style="{StaticResource MonthPeriodButtonStyle}"
                                    Command="{Binding SetMonthPeriodCommand}" />
                            <Button Content="Произвольно"
                                    Width="136"
                                    Margin="0,0,18,10"
                                    Style="{StaticResource CustomPeriodButtonStyle}"
                                    Command="{Binding SetCustomPeriodCommand}" />

                            <StackPanel Margin="0,0,16,10"
                                        Visibility="{Binding IsReferenceDateVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="Опорная дата"
                                           Style="{StaticResource MetricLabelTextStyle}"
                                           Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding ReferenceDate, Mode=TwoWay}"
                                            Style="{StaticResource DarkDatePickerStyle}" />
                            </StackPanel>

                            <StackPanel Margin="0,0,12,10"
                                        Visibility="{Binding IsCustomDatesVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="С"
                                           Style="{StaticResource MetricLabelTextStyle}"
                                           Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding CustomStartDate, Mode=TwoWay}"
                                            Style="{StaticResource DarkDatePickerStyle}" />
                            </StackPanel>

                            <StackPanel Margin="0,0,16,10"
                                        Visibility="{Binding IsCustomDatesVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="По"
                                           Style="{StaticResource MetricLabelTextStyle}"
                                           Margin="0,0,0,6" />
                                <DatePicker SelectedDate="{Binding CustomEndDate, Mode=TwoWay}"
                                            Style="{StaticResource DarkDatePickerStyle}" />
                            </StackPanel>

                            <StackPanel Margin="0,0,16,10"
                                        Visibility="{Binding IsReferenceDateVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <TextBlock Text="Диапазон"
                                           Style="{StaticResource MetricLabelTextStyle}"
                                           Margin="0,0,0,6" />
                                <Border Background="{StaticResource PanelActiveBrush}"
                                        BorderBrush="{StaticResource BorderBrushNeutral}"
                                        BorderThickness="1"
                                        CornerRadius="10"
                                        Padding="14,10"
                                        MinHeight="44"
                                        MinWidth="220">
                                    <TextBlock Text="{Binding PeriodLabel}"
                                               VerticalAlignment="Center"
                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                </Border>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal"
                                        VerticalAlignment="Bottom"
                                        Margin="0,0,12,10">
                                <Button Width="44"
                                        Content="←"
                                        Margin="0,0,8,0"
                                        Style="{StaticResource SecondaryActionButtonStyle}"
                                        Command="{Binding PreviousPeriodCommand}" />
                                <Button Width="44"
                                        Content="→"
                                        Margin="0,0,8,0"
                                        Style="{StaticResource SecondaryActionButtonStyle}"
                                        Command="{Binding NextPeriodCommand}" />
                                <Button Width="100"
                                        Content="Сегодня"
                                        Style="{StaticResource SecondaryActionButtonStyle}"
                                        Command="{Binding TodayCommand}" />
                            </StackPanel>

                            <Button Width="132"
                                    Margin="0,0,0,10"
                                    Content="Обновить"
                                    VerticalAlignment="Bottom"
                                    Style="{StaticResource PrimaryActionButtonStyle}"
                                    Command="{Binding LoadCommand}" />
                        </WrapPanel>
                    </StackPanel>
                </Border>

                <WrapPanel Grid.Row="2" Margin="0,0,0,20">
                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Width="260"
                            Height="126"
                            Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Сотрудников в выборке" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock Text="{Binding EmployeesCountDisplay}"
                                       Style="{StaticResource MetricValueTextStyle}"
                                       Foreground="{StaticResource AccentBrush}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Width="260"
                            Height="126"
                            Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Среднее продуктивное время" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock Text="{Binding AverageProductiveDisplay}"
                                       Style="{StaticResource MetricValueTextStyle}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Width="260"
                            Height="126"
                            Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Общее отслеженное время" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock Text="{Binding TotalTrackedDisplay}"
                                       Style="{StaticResource MetricValueTextStyle}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Width="260"
                            Height="126"
                            Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Общее время бездействия" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock Text="{Binding TotalIdleDisplay}"
                                       Style="{StaticResource MetricValueTextStyle}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Width="260"
                            Height="126"
                            Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Общее продуктивное время" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock Text="{Binding TotalProductiveDisplay}"
                                       Style="{StaticResource MetricValueTextStyle}" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Width="260"
                            Height="126"
                            Margin="0,0,16,16">
                        <StackPanel>
                            <TextBlock Text="Топ сотрудник" Style="{StaticResource MetricLabelTextStyle}" />
                            <TextBlock Text="{Binding TopEmployeeName}"
                                       Margin="0,10,0,0"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       TextWrapping="Wrap" />
                            <TextBlock Text="{Binding TopEmployeeMetric}"
                                       Margin="0,8,0,0"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </WrapPanel>

                <Border Grid.Row="3"
                        Margin="0,0,0,18"
                        Padding="14,12"
                        Background="{StaticResource PanelBrush}"
                        BorderBrush="{StaticResource BorderBrushNeutral}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Text="Загрузка сводной аналитики..."
                               Foreground="{StaticResource TextSecondaryBrush}" />
                </Border>

                <Border Grid.Row="4"
                        Margin="0,0,0,18"
                        Padding="16,14"
                        Background="#3D1C22"
                        BorderBrush="{StaticResource AppErrorBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel>
                        <TextBlock Text="Не удалось загрузить сводную аналитику"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Margin="0,10,0,0"
                                   TextWrapping="Wrap"
                                   Foreground="#FFD1D1" />
                    </StackPanel>
                </Border>

                <Grid Grid.Row="5">
                    <Border Style="{StaticResource PagePanelBorderStyle}"
                            Visibility="{Binding IsEmptyStateVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    MaxWidth="420">
                            <TextBlock Text="За выбранный период данных недостаточно"
                                       FontSize="24"
                                       FontWeight="SemiBold"
                                       TextAlignment="Center"
                                       Foreground="{StaticResource TextPrimaryBrush}" />
                            <TextBlock Text="Попробуйте выбрать другой период. Если активности мало или нет совсем, экран корректно покажет это состояние."
                                       Margin="0,12,0,0"
                                       TextAlignment="Center"
                                       TextWrapping="Wrap"
                                       Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                    </Border>

                    <Grid Visibility="{Binding HasAnyData, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Style="{StaticResource PagePanelBorderStyle}"
                                Margin="0,0,0,18">
                            <StackPanel>
                                <TextBlock Text="Динамика по дням"
                                           Style="{StaticResource SectionTitleTextStyle}" />
                                <TextBlock Text="Суммарное отслеженное время по доступной команде или компании."
                                           Style="{StaticResource SectionDescriptionTextStyle}"
                                           Margin="0,0,0,18" />

                                <ItemsControl ItemsSource="{Binding DailyBuckets}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Rows="1" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="6,0,6,0"
                                                  VerticalAlignment="Bottom">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto" />
                                                    <RowDefinition Height="130" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Row="0"
                                                           Text="{Binding TotalDisplay}"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,0,0,10"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                                <Border Grid.Row="1"
                                                        Width="34"
                                                        Height="{Binding BarHeight}"
                                                        VerticalAlignment="Bottom"
                                                        HorizontalAlignment="Center"
                                                        Background="{StaticResource AccentBrush}"
                                                        CornerRadius="8,8,0,0" />
                                                <TextBlock Grid.Row="2"
                                                           Text="{Binding Label}"
                                                           HorizontalAlignment="Center"
                                                           Margin="0,10,0,0"
                                                           FontSize="12"
                                                           Foreground="{StaticResource TextSecondaryBrush}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Style="{StaticResource PagePanelBorderStyle}"
                                    Margin="0,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="Топ сотрудников"
                                               Style="{StaticResource SectionTitleTextStyle}" />
                                    <ItemsControl ItemsSource="{Binding TopEmployees}" Margin="0,18,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,10"
                                                        Padding="12"
                                                        Background="{StaticResource PanelActiveBrush}"
                                                        BorderBrush="{StaticResource BorderBrushNeutral}"
                                                        BorderThickness="1"
                                                        CornerRadius="8">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*" />
                                                            <ColumnDefinition Width="Auto" />
                                                        </Grid.ColumnDefinitions>
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding FullName}" FontWeight="SemiBold" TextWrapping="Wrap" />
                                                            <TextBlock Text="{Binding Role}" Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" />
                                                        </StackPanel>
                                                        <StackPanel Grid.Column="1" Margin="18,0,0,0">
                                                            <TextBlock Text="{Binding ProductiveDisplay}" HorizontalAlignment="Right" FontWeight="SemiBold" />
                                                            <TextBlock Text="{Binding TotalDisplay}" Margin="0,6,0,0" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Right" />
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1"
                                    Style="{StaticResource PagePanelBorderStyle}"
                                    Margin="12,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="Топ приложений"
                                               Style="{StaticResource SectionTitleTextStyle}" />
                                    <ItemsControl ItemsSource="{Binding Applications}" Margin="0,18,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,10"
                                                        Padding="12"
                                                        Background="{StaticResource PanelActiveBrush}"
                                                        BorderBrush="{StaticResource BorderBrushNeutral}"
                                                        BorderThickness="1"
                                                        CornerRadius="8">
                                                    <DockPanel>
                                                        <TextBlock Text="{Binding DurationDisplay}" DockPanel.Dock="Right" Foreground="{StaticResource TextSecondaryBrush}" />
                                                        <TextBlock Text="{Binding Name}" TextWrapping="Wrap" FontWeight="SemiBold" />
                                                    </DockPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2"
                                    Style="{StaticResource PagePanelBorderStyle}"
                                    Margin="12,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="Категории"
                                               Style="{StaticResource SectionTitleTextStyle}" />
                                    <ItemsControl ItemsSource="{Binding Categories}" Margin="0,18,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Margin="0,0,0,10"
                                                        Padding="12"
                                                        Background="{StaticResource PanelActiveBrush}"
                                                        BorderBrush="{StaticResource BorderBrushNeutral}"
                                                        BorderThickness="1"
                                                        CornerRadius="8">
                                                    <DockPanel>
                                                        <TextBlock Text="{Binding DurationDisplay}" DockPanel.Dock="Right" Foreground="{StaticResource TextSecondaryBrush}" />
                                                        <TextBlock Text="{Binding Name}" TextWrapping="Wrap" FontWeight="SemiBold" />
                                                    </DockPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
