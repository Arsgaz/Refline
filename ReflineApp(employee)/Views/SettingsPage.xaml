<Page x:Class="Refline.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#0B1220">

    <Page.Resources>
        <SolidColorBrush x:Key="PageBaseBrush" Color="#0B1220"/>
        <SolidColorBrush x:Key="PageSecondaryBrush" Color="#10192B"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#111827"/>
        <SolidColorBrush x:Key="PanelHoverBrush" Color="#162033"/>
        <SolidColorBrush x:Key="PanelActiveBrush" Color="#11263A"/>
        <SolidColorBrush x:Key="BorderBrushNeutral" Color="#1E2A3D"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#DDE6F1"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#94A3B8"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#2DC7FF"/>
        <SolidColorBrush x:Key="AccentHoverBrush" Color="#63D8FF"/>

        <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0B1220" Offset="0"/>
            <GradientStop Color="#10192B" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="PanelBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="22"/>
        </Style>

        <Style x:Key="SectionTitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="SectionDescriptionTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="ReflineActionButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                CornerRadius="10"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PanelHoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PanelActiveBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ReflineActionButton}">
            <Setter Property="MinWidth" Value="240"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelActiveBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
        </Style>

        <Style x:Key="SecondaryActionButtonStyle" TargetType="Button" BasedOn="{StaticResource ReflineActionButton}">
            <Setter Property="MinWidth" Value="240"/>
            <Setter Property="Height" Value="48"/>
        </Style>

        <Style x:Key="PathTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Background" Value="{StaticResource PanelActiveBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushNeutral}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
        </Style>

        <Style x:Key="FolderButtonStyle" TargetType="Button" BasedOn="{StaticResource ReflineActionButton}">
            <Setter Property="Width" Value="128"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        </Style>

        <Style x:Key="SettingsCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Border x:Name="CheckBorder"
                                        Width="18"
                                        Height="18"
                                        CornerRadius="4"
                                        Background="{StaticResource PanelActiveBrush}"
                                        BorderBrush="{StaticResource BorderBrushNeutral}"
                                        BorderThickness="1"
                                        Margin="0,1,12,0">
                                    <Path x:Name="CheckMark"
                                          Data="M 3 9 L 7 13 L 15 4"
                                          Stroke="{StaticResource AccentBrush}"
                                          StrokeThickness="2"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"
                                          StrokeLineJoin="Round"
                                          Visibility="Collapsed"/>
                                </Border>
                            </BulletDecorator.Bullet>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </BulletDecorator>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background" Value="{StaticResource PanelActiveBrush}"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{StaticResource PageBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <Grid Margin="32">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,28">
                    <TextBlock Text="Настройки"
                               FontSize="30"
                               FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}"
                               Margin="0,0,0,8"/>

                    <TextBlock Text="Управляйте параметрами приложения, путями сохранения и корпоративным доступом."
                               FontSize="15"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="1"
                            Width="760"
                            HorizontalAlignment="Center">
                    <Border Style="{StaticResource PanelBorderStyle}"
                            Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="Путь сохранения отчётов"
                                       Style="{StaticResource SectionTitleTextStyle}"/>

                            <TextBlock Text="Укажите папку, куда будут сохраняться PDF и XLS отчёты."
                                       Style="{StaticResource SectionDescriptionTextStyle}"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="12"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBox x:Name="PathBox"
                                         Grid.Column="0"
                                         Text="{Binding ReportsPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Style="{StaticResource PathTextBoxStyle}"/>

                                <Button Grid.Column="2"
                                        Content="Выбрать папку"
                                        Command="{Binding SelectFolderCommand}"
                                        Style="{StaticResource FolderButtonStyle}"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource PanelBorderStyle}"
                            Margin="0,0,0,24">
                        <StackPanel>
                            <TextBlock Text="Системные параметры"
                                       Style="{StaticResource SectionTitleTextStyle}"/>

                            <TextBlock Text="Настройка автозапуска, фонового режима и логирования."
                                       Style="{StaticResource SectionDescriptionTextStyle}"/>

                            <CheckBox Content="Запускать Refline при старте Windows"
                                      IsChecked="{Binding AutoStartWindows, Mode=TwoWay}"
                                      Style="{StaticResource SettingsCheckBoxStyle}"/>

                            <CheckBox Content="Разрешить фоновый сбор активности"
                                      IsChecked="{Binding AllowBackgroundTracking, Mode=TwoWay}"
                                      Style="{StaticResource SettingsCheckBoxStyle}"/>

                            <CheckBox Content="Вести локальный лог работы приложения"
                                      IsChecked="{Binding EnableLocalLog, Mode=TwoWay}"
                                      Style="{StaticResource SettingsCheckBoxStyle}"
                                      Margin="0"/>
                        </StackPanel>
                    </Border>

                    <StackPanel HorizontalAlignment="Center">
                        <Button Content="Сохранить настройки"
                                Command="{Binding SaveSettingsCommand}"
                                Style="{StaticResource PrimaryActionButtonStyle}"/>

                        <Button Content="Выйти из аккаунта"
                                Command="{Binding LogoutCommand}"
                                Style="{StaticResource SecondaryActionButtonStyle}"
                                Margin="0,14,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
